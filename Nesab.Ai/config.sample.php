<?php
/**
 * Nesab AI — Server Configuration
 *
 * INSTRUCTIONS:
 * 1. Rename this file to config.php
 * 2. Fill in your OpenAI API key below
 * 3. Upload config.php to the same directory as chat.php on the server
 * 4. NEVER commit config.php to git — add it to .gitignore
 * 5. NEVER place this file in any public frontend bundle
 */

// Your OpenAI API key — server-side only, never exposed to the frontend
define('OPENAI_API_KEY', 'sk-REPLACE-WITH-YOUR-KEY');